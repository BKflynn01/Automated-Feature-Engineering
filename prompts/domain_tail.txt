Generate "Alpha Factors" based on the <Domain Heuristics> and remove redundant features.
Prioritize stationary metrics (e.g., Z-Scores, Log-Returns) over raw prices.

First, provide a one-sentence "Alpha Hypothesis" in the docstring explaining the financial rationale.
Then, implement the `modify_features_v2` function.

**CRITICAL INSTRUCTION**
1. Verify NO look-ahead bias (e.g., no `shift(-1)`).
2. Use the score of the following programs as a reference but **never** make up your own score.
3. Your code must start with df_output = df_input.copy() before any alterations are made